asgardr-dev-assets:
  image: 'turistforeningen/asgardr-dev:assets'
  tags:
    - app
    - volume-2
  volumes:
    - /assets
  command: /bin/true
  restart: always
asgardr-dev-node:
  image: 'turistforeningen/asgardr-dev:node'
  autoredeploy: true
  environment:
    - 'APP_URL=https://admin.nasjonalturbase.no'
    - FORCE_SSL=true
    - HSTS_MAX_AGE=31536000
    - NODE_ENV=production
    - 'NTB_API_URL=https://dev.nasjonalturbase.no'
    - NTB_API_ENV=dev
    - 'VIRTUAL_HOST=http://admin.nasjonalturbase.no*,https://admin.nasjonalturbase.no*'
    - VIRTUAL_HOST_WEIGHT=20
    - VIRTUAL_PATH=/
    - VIRTUAL_PORT=8080
  expose:
    - '8080'
  links:
    - 'asgardr-dev-redis:redis'
    - 'statsd.statsd-librato:statsd'
  restart: always
  command: node index.js
  tags:
    - app
    - volume-2
  volumes_from:
    - 'asgardr-dev-assets:ro'
    - 'secrets:ro'
asgardr-dev-redis:
  autoredeploy: true
  command: redis-server --appendonly yes
  image: 'redis:3.0'
  restart: on-failure
  tags:
    - app
    - volume-2
secrets:
  image: 'turistforeningen/asgardr-secrets:dev'
  tags:
    - app
    - volume-2
  volumes:
    - /secrets
